#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    // + addition
    // - subtraction
    // * multiplication
    // / division

    int x = 3;
    int y = 2;
    int z = 5;

    printf("%f", pow(x, y));

    // ~~ With <math.h> ~~
    // floor(x) rounds down
    // ceil(x) rounds up
    // pow(x, y) brings x to the y power
    // sqrt(x) finds square root

    return 0;
}